import { name } from "../Index"

export const Expenses = () => {

    return(
        <body>
            <div class="greeting-text">
                <span>Welcome Back!</span>
            </div>
        </body>
    )
}